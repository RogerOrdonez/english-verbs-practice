import tw from 'twin.macro'

export const PracticeFormHeaderMessage = () => {
  return (
    <div
      css={tw`text-gray-400 dark:text-gray-100 uppercase tracking-wider text-sm lg:text-base`}
    >
      Complete the following fields
    </div>
  )
}
